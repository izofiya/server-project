style
    include ../public/css/main.css
header.header
content.container
  div.profile
    img.profile-img(src=`/static/assets/images/${person.imgSrc}`, alt='imagePerson')
    div.profile-details
      h1
        | #{person.firstname} #{person.lastname}
      h2
        | #{person.tagline}
      a.button.button-facebook(href=`${person.facebook}`)
        | Follow me on Facebook
        